# Database Configuration (PostgreSQL)
# For local development, use: postgresql://username:password@localhost:5432/database_name
# For Render deployment, use the internal DATABASE_URL from Render dashboard
DATABASE_URL=postgresql://username:password@localhost:5432/healthy_meal_planner

# Server Configuration
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# JWT Secret (Change this to a random string)
JWT_SECRET=your_jwt_secret_key_here

# Email Configuration (Optional - for bonus feature)
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_app_password
